#include <iostream>

using namespace std;

int main()
{
    int a, b, c;
    cin >> a >> b;
    c = a + b;
    if(c > 10 || c == 10) {
        cout << "C is bigger or equal to 10";
    }

    return 0;
}
